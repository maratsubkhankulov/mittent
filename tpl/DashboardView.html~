<div class="panel panel-default">
	<div class="panel-body" >
		<form class="form-horizontal" role="form">
			<div class="form-group">
				<label for="inputEmail3" class="col-sm-2 control-label">Public Link</label>
				<div class="col-sm-7">
					<input type="email" class="form-control" id="inputEmail3" value="<%= url %>#view/<%= id %>">
				</div>
			</div>
			<div class="form-group">
				<label for="inputPassword3" class="col-sm-2 control-label">Start</label>
				<div class="col-sm-7">
					<input type="date" class="form-control" id="inputStartDate" value="<%= directory.newDate(startDate).toISOString().substring(0, 10) %>">
					<p id='startDateWarn' style="display: none">Invalid start date</p>
				</div>
			</div>
			<div class="form-group">
				<label for="inputPassword3" class="col-sm-2 control-label">End</label>
				<div class="col-sm-7">
					<input type="date" class="form-control" id="inputEndDate" value="<%= directory.newDate(endDate).toISOString().substring(0, 10) %>">
					<p id='endDateWarn' style="display: none">Invalid end date</p>
				</div>
			</div>
			<div class="form-group">
				<label class="col-sm-2 control-label"></label>
				<div class="col-sm-7">
				<a id='updateBtn' class="btn btn-default">Update</a>
			</div>
		</form>
    <div class="col-sm-1">
        <table class="table table-hover"id="days">
          <thead>
            <tr>
              <th>Live</th>
              <th>Date</th>
              <th>Viewed</th>
              <th>Preview</th>
            </tr>
          </thead>
        </table>
	</div>
</div>
